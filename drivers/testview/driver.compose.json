{
  "name": {
    "en": "Test View"
  },
  "class": "sensor",
  "capabilities": [
    "measure_temperature"
  ],
  "capabilitiesOptions": {
    "measure_temperature": {
      "title": {
        "en": "Measured Temperature"
      },
      "min": -10,
      "max": 50,
      "unit": "°C",
      "description": {
        "en": "Displays the measured temperature"
      }
    }
  },
  "platforms": ["local"],
  "connectivity": ["lan"],
  "pair": [
    {
      "id": "minicust",
      "template": "custom",
      "options": {
        "html": "{{driverAssetsPath}}/pair/minicust.html"
      },
      "next": "list_devices"
    },
    {
      "id": "list_devices",
      "template": "list_devices",
      "options": {
        "list_devices": {
          "generate": true
        }
      },
      "next": "add_devices"
    },
    {
      "id": "add_devices",
      "template": "add_devices"
    }
  ]
}
